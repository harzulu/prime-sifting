$(document).ready(function() {

// form submission
    $("#number").submit((e) => {
        e.preventDefault();
    // collect user input
        let originalNumber = parseInt($("#number-input").val());
        console.log()

    })
});